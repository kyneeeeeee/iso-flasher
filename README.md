# iso-flasher

Programa para flashear ISOs en USBs desde la terminal.

## Instalación

git clone https://github.com/tu-usuario/iso-flasher
cd iso-flasher
cargo install --path .
sudo cp ~/.cargo/bin/iso-flasher /usr/local/bin/

## Uso

sudo iso-flasher
